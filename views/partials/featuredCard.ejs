<div class="col-md-4">
  <a
    href="#"
    class="card-link"
  >
    <div class="card">
      <img
        src="<%= category.image %>"
        class="card-img"
        alt="<%= category.name %>"
      />
      <h5 class="card-title"><%= category.name %></h5>
    </div>
  </a>
</div>
