<section>
    <table class="table table-hover">
        <thead>
            <tr>
                <th>ASMRtist</th>
            </tr>
        </thead>
        <tbody>
            <% asmrtists.forEach((asmrtist) => { %>
                <tr>
                    <td><%= asmrtist.name %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</section>